# 🎨 应用配色方案 - 绿色系

## 当前配色（绿色健康主题）

### 主要颜色

#### 背景渐变
- 起始色: `#11998e` (青绿色)
- 结束色: `#38ef7d` (亮绿色)
- 效果: 清新、健康、活力

```css
background: linear-gradient(135deg, #11998e 0%, #38ef7d 100%);
```

#### 打卡按钮
- 起始色: `#56ccf2` (天蓝色)
- 结束色: `#2f80ed` (宝蓝色)
- 阴影: `rgba(47, 128, 237, 0.4)`

```css
background: linear-gradient(135deg, #56ccf2 0%, #2f80ed 100%);
```

#### 主题色
- 数字统计: `#11998e` (青绿色)
- 进度环: `#11998e` (青绿色)
- 记录序号: `#11998e` (青绿色)

### 辅助颜色

- 卡片背景: `#ffffff` (白色)
- 主要文字: `#333333` (深灰)
- 次要文字: `#666666` (中灰)
- 辅助文字: `#999999` (浅灰)
- 清空按钮: `#ff4757` (红色)

## 配色理念

### 绿色主题的优势
1. 💚 **健康感** - 绿色代表健康、生命
2. 💧 **清新感** - 符合"喝水"主题
3. 🌿 **自然感** - 接近大自然的颜色
4. 👁️ **护眼** - 绿色对眼睛友好
5. ✨ **活力感** - 明亮的绿色充满能量

### 配色对比

| 元素 | 原紫色系 | 新绿色系 |
|------|---------|---------|
| 背景 | 紫色渐变 | 青绿渐变 |
| 按钮 | 粉红渐变 | 蓝绿渐变 |
| 主题色 | 紫色 | 青绿色 |
| 风格 | 时尚梦幻 | 清新健康 |

## 其他配色方案参考

如果你想尝试其他颜色，可以修改以下文件：
- `test.html` - 浏览器测试版
- `www/css/index.css` - Cordova 应用版

### 配色方案一：深绿系（森林）
```css
/* 背景 */
background: linear-gradient(135deg, #56ab2f 0%, #a8e063 100%);

/* 按钮 */
background: linear-gradient(135deg, #36d1dc 0%, #5b86e5 100%);

/* 主题色 */
color: #56ab2f;
```

### 配色方案二：蓝绿系（海洋）
```css
/* 背景 */
background: linear-gradient(135deg, #2193b0 0%, #6dd5ed 100%);

/* 按钮 */
background: linear-gradient(135deg, #00d2ff 0%, #3a7bd5 100%);

/* 主题色 */
color: #2193b0;
```

### 配色方案三：浅绿系（清爽）
```css
/* 背景 */
background: linear-gradient(135deg, #90ee90 0%, #7fffd4 100%);

/* 按钮 */
background: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%);

/* 主题色 */
color: #00bfa5;
```

### 配色方案四：原紫色系（恢复）
如果想恢复原来的紫色系：

```css
/* 背景 */
background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);

/* 按钮 */
background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);

/* 主题色 */
color: #667eea;
```

## 如何修改颜色

### 方法一：修改 test.html（浏览器测试）

1. 打开 `test.html`
2. 找到 `<style>` 标签内的颜色值
3. 替换为你喜欢的颜色
4. 保存并刷新浏览器

### 方法二：修改 www/css/index.css（手机应用）

1. 打开 `www/css/index.css`
2. 搜索颜色代码（如 `#11998e`）
3. 替换为新颜色
4. 重新构建应用

## 颜色工具推荐

### 渐变色生成
- https://cssgradient.io/
- https://uigradients.com/
- https://gradients.app/

### 配色方案
- https://coolors.co/
- https://colorhunt.co/
- https://flatuicolors.com/

### 取色工具
- ColorZilla (浏览器插件)
- Adobe Color
- Paletton

## 颜色代码快速参考

### 绿色系
- `#00b894` 清新绿
- `#00cec9` 青色
- `#00b09b` 深青绿
- `#96c93d` 草绿
- `#38ef7d` 亮绿

### 蓝色系
- `#0984e3` 蓝色
- `#74b9ff` 浅蓝
- `#2f80ed` 宝蓝
- `#56ccf2` 天蓝

### 辅助色
- `#ff4757` 红色（警告、删除）
- `#ffa502` 橙色（提示）
- `#2ed573` 绿色（成功）

## 快速测试

修改完颜色后：

1. 打开 `test.html` 查看效果
2. 如果满意，确保同步修改 `www/css/index.css`
3. 重新构建应用查看手机效果

---

**当前版本**: 绿色健康主题 v1.0
**更新日期**: 2024-02-01
